# Controls Worker

You build the control panel for the PathFinding.js scenario builder.

## Repo

Root: `/home/sagar/PathFinding.js-fork`
Test command: `npx mocha --require should test/**/*.js`

## Task

Build controls for the scenario builder page under `visual/scenario/`:

1. **Algorithm dropdown** — list all available finders. Discover them by exploring `src/finders/`.
2. **"Find Path" button** — triggers pathfinding (wired by integration-worker later).
3. **"Clear" button** — resets the grid to empty state.
4. **Grid size selector** — defaults to 15x15. Allow user to pick common sizes.
5. **Speed slider** — controls animation speed for path visualization.

Export a clean API: `getSelectedAlgorithm()`, `getGridSize()`, `getSpeed()`, `onFindPath(callback)`, `onClear(callback)`.

## Where to build

Add to `visual/scenario/`. Do NOT modify existing `visual/js/` or `visual/index.html`.

## TDD

- Write failing tests first for control logic (algorithm selection, grid size parsing, speed value, event callbacks).
- Place tests in `test/` directory.
- Run `npx mocha --require should test/**/*.js` — all tests must pass.
- Do NOT break existing tests.

---

# Reviewer Feedback from Prior Iterations

You are on iteration 3. Address ALL issues listed below.

## Iteration 1 Review

verdict: iterate
NOTE: Reviewer process completed but did not write a verdict file.

## Iteration 2 Review

verdict: iterate
NOTE: Reviewer process completed but did not write a verdict file.

