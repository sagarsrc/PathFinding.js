# Integration Worker

You wire all scenario builder components together into a working app.

## Repo

Root: `/home/sagar/PathFinding.js-fork`
Test command: `npx mocha --require should test/**/*.js`

## Task

Wire everything under `visual/scenario/`:

1. **Create `visual/scenario/index.html`** — the scenario builder entry point that loads all components.
2. **Wire canvas + controls** — "Find Path" button reads grid state and selected algorithm, runs pathfinding.
3. **Hook pathfinding** — use the PathFinding.js library (`src/` or the built `lib/pathfinding-browser.min.js`) to run the selected algorithm on the current grid.
4. **Animate path** — step-by-step visualization of the pathfinding process (nodes explored, then final path).
5. **Wire scorer** — after each run, capture metrics and display score card.
6. **Wire persistence** — save/load buttons use persistence module, preset selector loads built-in maps.
7. **Wire comparison** — user can save runs and open comparison drawer.

Explore what the other workers built in `visual/scenario/` and wire it together. If components expose different APIs than expected, adapt.

## Where to build

Edit/create files in `visual/scenario/`. Do NOT modify existing `visual/js/` or `visual/index.html`.

## TDD

- Write integration tests: run algorithm on grid → get path → metrics captured → save run → load run.
- Place tests in `test/` directory.
- Run `npx mocha --require should test/**/*.js` — all tests must pass.
- Do NOT break existing tests.

---

# Reviewer Feedback from Prior Iterations

You are on iteration 2. Address ALL issues listed below.

## Iteration 1 Review

verdict: iterate
NOTE: Reviewer process completed but did not write a verdict file.

